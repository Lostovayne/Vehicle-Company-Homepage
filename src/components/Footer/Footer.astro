---
import Image from "astro/components/Image.astro";
import Button from "../Reutilizables/Button.astro";
import { FooterLinks } from "../../constants/footer";
---

<footer class="w-full py-20 text-white bg-[#151515]">
  <section class="mx-auto max-w-screen-2xl flex flex-col">
    <!-- top section -->
    <section class="flex flex-col sm:flex-row mb-20">
      <!-- left side -->
      <div class="w-full sm:w-1/2 p-5 flex flex-col gap-y-10">
        <!-- title -->
        <h2 class="text-4xl font-normal text-pretty max-w-xl">
          Quality means doing it right when no one is looking
          <cite class="text-sm text-[#848484]">– Henry Ford</cite>
        </h2>
        <p class="text-lg text-pretty max-w-xl">
          Our dealership takes your privacy seriously and is committed to
          safeguarding your privacy online. Because we do gather certain types
          of information from you.
        </p>
        <!-- button -->

        <Button text="Contact For Queries" />
      </div>
      <!-- right side -->
      <div class="w-full sm:w-1/2 flex items-center justify-end">
        <Image
          class="w-full max-w-[460px] max-h-[450px] object-cover object-top xl:hidden"
          src="/footerCar.svg"
          alt="Mustang Car"
          width={500}
          height={500}
        />
        <Image
          class="hidden xl:block w-full"
          src="/footerFullCar.svg"
          alt="Car"
          width={1366}
          height={584}
        />
      </div>
    </section>
    <!-- bottom section -->
    <section class="px-5 pt-10">
      <!-- links -->
      <ul
        class="flex flex-col items-center md:flex-row md:justify-center md:items-center gap-6 text-current"
      >
        <!-- logo -->
        <li>
          <a href="#"
            ><Image
              class={"w-52 md:w-20 md:mr-5"}
              src="/logo.svg"
              width={100}
              height={50}
              alt="Ford logo"
            /></a
          >
        </li>
        {
          FooterLinks.map((item) => (
            <li class="">
              <a href={item.path} class="text-base font-normal ">
                {item.name}
              </a>
            </li>
          ))
        }
        <!-- social icons -->
        <li class="flex [&>a>img]:w-5 [&>a>img]:h-5 gap-x-3 md:ml-5">
          <a href="#"><img src="/src/icons/facebook.svg" alt="facebook" /></a>
          <a href="#"><img src="/src/icons/instagram.svg" alt="facebook" /></a>
          <a href="#"><img src="/src/icons/youtube.svg" alt="facebook" /></a>
        </li>
      </ul>
      <!-- divider -->
      <div class="h-px w-full bg-[#2C2C2C] my-10"></div>
      <span class="text-sm text-[#4F4F4F] block text-center"
        >Copyright &copy; 2024. All rights reserved.</span
      >
    </section>
  </section>
</footer>
